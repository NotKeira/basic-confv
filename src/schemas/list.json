{
  "javascript": {
    "nodejs": {
      "package.json": {
        "required": {
          "name": "string",
          "version": "string"
        },
        "optional": {
          "description": "string",
          "dependencies": "object",
          "devDependencies": "object",
          "scripts": "object",
          "main": "string",
          "author": "string",
          "license": "string",
          "keywords": "array",
          "repository": "object",
          "bugs": "object",
          "homepage": "string"
        }
      }
    }
  },
  "webserver": {
    "nginx": {
      "nginx.conf": {
        "required": {
          "http": "object",
          "server": "object"
        },
        "optional": {
          "events": "object",
          "worker_processes": "string",
          "error_log": "string",
          "pid": "string",
          "user": "string"
        }
      }
    }
  },
  "containers": {
    "docker": {
      "docker-compose.yml": {
        "required": {
          "version": "string",
          "services": "object"
        },
        "optional": {
          "volumes": "object",
          "networks": "object",
          "configs": "object",
          "secrets": "object"
        }
      }
    }
  },
  "configuration": {
    "yaml": {
      "config.yaml": {
        "required": {
          "settings": "object",
          "enabled": "boolean"
        },
        "optional": {
          "metadata": "object",
          "logging": "object",
          "version": "string",
          "environment": "string"
        }
      }
    }
  }
}